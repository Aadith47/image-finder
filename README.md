# Image Finder

A Python CLI tool that uses AI to understand natural-language image requirements and find relevant images using the Pexels API.

## Overview

Instead of searching with simple keywords, Image Finder allows users to describe the image they need in natural language.

Example:

## text
"A coffee on a table with GitHub open on a monitor"
